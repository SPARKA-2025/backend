APP_NAME=SPARKA
APP_ENV=development
APP_KEY=base64:dDVqLtbni8vrGoEjC2RxzasSvTrDmh5EpKSL4ItiB2o=
APP_DEBUG=true
APP_URL=http://localhost:8000
APP_TIMEZONE=Asia/Jakarta

LOG_CHANNEL=stack
LOG_SLACK_WEBHOOK_URL=

# Database Configuration (Docker)
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=sparka_db
DB_USERNAME=sparka_user
DB_PASSWORD=sparka123

# Cache Configuration (Docker)
CACHE_DRIVER=file
QUEUE_CONNECTION=rabbitmq

# Redis Configuration (Docker)
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_CLIENT=predis
REDIS_DB=0
REDIS_CACHE_DB=1

# Session Configuration
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Queue Connection for RabbitMQ (Docker)
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=sparka
RABBITMQ_PASSWORD=sparka123
RABBITMQ_VHOST=/

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=noreply@sparka.com
MAIL_FROM_NAME="${APP_NAME}"

# Streaming Configuration
STREAMING_API_URL=http://sparka-streaming:8010
STREAMING_API_INTERNAL_URL=http://sparka-streaming:8010

# JWT Configuration
JWT_SECRET=yRQKLSRIRNX8jg4e2Er1duoM4Cj3UlHgWIrETHkh7ZalYAFETWSKwAeXlGqLKfaX
JWT_TTL=60
JWT_REFRESH_TTL=20160

# AI Service Configurations (Docker Services)
AI_API_URL=http://ai-api:5000
AI_PREDICT_URL=http://ai-api:5000/predict
AI_API_TIMEOUT=30
GRPC_OCR_HOST=grpc-ocr
GRPC_OCR_PORT=50052
GRPC_PLATE_HOST=grpc-plate
GRPC_PLATE_PORT=50053
GRPC_VEHICLE_HOST=grpc-vehicle
GRPC_VEHICLE_PORT=50051

# Integration Services (Docker)
CCTV_INTEGRATION_URL=http://sparka-integration:8001
FILE_PROCESSOR_URL=http://file-processor:8002
WEBSOCKET_SERVICE_URL=http://websocket-service:8003
INTEGRATION_HUB_URL=http://sparka-integration:8004

# Storage Configuration (Docker MinIO)
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=sparka_access
MINIO_SECRET_KEY=sparka_secret
MINIO_BUCKET=sparka-bucket
MINIO_USE_SSL=false

# Monitoring Services (Docker)
INFLUXDB_URL=http://influxdb:8086
INFLUXDB_TOKEN=your_influxdb_token
INFLUXDB_ORG=sparka
INFLUXDB_BUCKET=sparka_metrics
GRAFANA_URL=http://grafana:3001

# CORS Configuration
CORS_ALLOWED_ORIGINS=*

# File Upload Configuration
FILESYSTEM_DRIVER=local
MAX_UPLOAD_SIZE=10240
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif,bmp,webp
ALLOWED_VIDEO_TYPES=mp4,avi,mov,mkv,webm

# API Rate Limiting
API_RATE_LIMIT=60
API_RATE_LIMIT_WINDOW=1

# Logging Configuration
LOG_LEVEL=info
LOG_DEPRECATIONS_CHANNEL=null
LOG_STACK=single

# Broadcasting
BROADCAST_DRIVER=log

# Queue
QUEUE_CONNECTION=rabbitmq

# Cache
CACHE_DRIVER=file

# Session
SESSION_DRIVER=file
SESSION_LIFETIME=120

# Sanctum
SANCTUM_STATEFUL_DOMAINS=localhost,localhost:3000,127.0.0.1,127.0.0.1:8000,::1

# Application Specific
PARKING_SLOT_TIMEOUT=300
CCTV_CONNECTION_TIMEOUT=30
STREAM_CLEANUP_INTERVAL=3600
AUTO_CLEANUP_ENABLED=true

# Development/Debug
TELESCOPE_ENABLED=false
DEBUGBAR_ENABLED=false